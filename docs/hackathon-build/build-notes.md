# Hackathon Build Notes

## 2026-08-27 — Guided interview started

- Official Devpost rules were acknowledged with an explicit `yes`.
- The founder requested questions one at a time with options and durable recording after each answer.
- Active shaping: the founder's one-question-at-a-time instruction supersedes the guided tool's default batched interview format.
- Current concept: human-teachable, revision-safe accessibility repair through WebMCP.
- No implementation has been authorized or started in this workflow.

## 2026-08-28 — D-002 primary user

- Founder selected option A.
- Primary user: accessibility or design-system lead inside one product company.
- Scope implication: optimize for recurring review of similar components within one product; exclude agency multi-tenancy from the MVP.

## 2026-08-28 — D-003 mutation target

- Founder selected option A.
- Approved repairs persist as revisioned application-owned section records and immediately drive the live page.
- Required invariants: stale-revision rejection, read-after-write verification, and revision-based undo.
- Excluded from MVP: source-code patch/deployment and browser-only temporary changes.

## 2026-08-28 — Real-time research and decision tournament

- Refreshed the live Devpost event through the Devpost Hackathons connector and cross-checked formal rules, resources, updates, and the unavailable gallery.
- Verified current OpenAI Site tools, Chrome WebMCP, the August 26 draft specification, browser standards positions, current security issues, and the August 27 W3C APA accessibility objection.
- Completed a strict 30-day market pulse across Reddit, GitHub, product documentation/reviews, X, Hacker News, and YouTube; recorded source coverage gaps rather than treating failed/empty searches as market silence.
- Audited the current clean Clivus runtime checkout read-only. Reuse verdict: temporal/provenance/supersession concepts and golden-set evaluation patterns only; full runtime/server/daemon/vector/graph/private corpus rejected; RRF deferred and NOT TESTED for this product.
- Ran the full decision tournament: exactly ten generated candidates, three anonymized finalists, five judge roles, a top-two rematch, one synthesizer, and one fresh-eyes verifier.
- Provisional paper-tournament result: C08/T-B/R-1, originally “FocusRoute Studio,” renamed “Focus Contract Studio,” won the rematch 5–0 with an aggregate of criterion medians of approximately 8.0/10.
- Status remains `PROVISIONAL TOURNAMENT CHAMPION — SPIKE-ONLY — HARD GATES PENDING — FOUNDER DECISION REQUIRED`. It is not a confirmed founder choice and not an implementation-ready validation result.
- Fallback: Responsive Semantic-Order Relay. It has lower focus-runtime complexity but remains independently unvalidated; shared WebMCP-client, authorization, packaging, or overall schedule failures kill both concepts.
- No implementation code was created. The next action remains one founder decision with mutually exclusive options.

## 2026-08-29 — Product and RRF scope locked; build authorized

- Founder accepted the smaller-but-still-sealed RRF proof: 36 records, 12 development queries, and 18 frozen holdout queries.
- The benchmark remains a release blocker; retrieval cannot convey approval or mutation authority.
- Founder directed the workflow to stop prolonging discussion and create the complete build authority for autonomous end-to-end implementation.
- Prior selections were reconciled into the decision log: Focus Contract Studio, ChatGPT Sites plus D1, anonymous judge mode plus optional Sign in with ChatGPT, no model API, Apache-2.0, individual submission, 45–60 focused hours, two adversarial reviews, and the decision-memory hero proof.
- Real-time stack research selected the current official ChatGPT Sites starter, strict TypeScript, raw D1 prepared statements and guarded batches, eligible-only TypeScript BM25 plus structured/subject-edge ranks and clean-room RRF, Zod-generated WebMCP schemas, Vitest/Cloudflare integration tests, and Playwright/axe plus real-client acceptance.
- Active shaping: the founder explicitly chose autonomous completion over more interviews. The guided workflow therefore produced scope, PRD, spec, checklist, and the deeper implementation contracts in one evidence-backed pass.

## 2026-08-29 — Authority revision 2 and benchmark seal

- Corrected the hero so the active revision is always the renderer's implemented configuration: revision 1 renders Delete, eligible precedent D001 says Cancel, and the UI reports `DECISION MISMATCH`. No hidden desired contract remains.
- Replaced the FTS5 production rank with deterministic TypeScript BM25 over the already-eligible maximum 36 rows because FTS5 collection statistics can be influenced by ineligible rows.
- Preserved v1 as an invalid preregistration after its strongest baseline was mechanically shown to make the lift target impossible.
- Created and sealed benchmark v2 with schemas, deterministic answer-neutral queries, whole-field corpus overrides, reference evaluator, calibration record, and SHA-256 manifest. The pre-seal reference run passes all frozen feasibility gates; product implementation must still independently match development goldens and run the release holdout once on source commit `C` after public deployment.
- Corrected Sites release semantics: every deployment is production, saved versions do not promise isolated D1 state, migrations remain additive, and post-deploy evidence belongs in a separate immutable release attestation rather than inside the deployed source commit.
- Documentation is now implementation authority; historical tournament labels above are provenance only and no longer describe current product status.
